{
    "name": "modpackresolver",
    "type": "module",
    "workspaces": [
        "backend",
        "cli",
        "mclib",
        "web"
    ],
    "scripts": {
        "lint": "set -xe; (cd backend && bun run lint) && (cd cli && bun run lint) && (cd mclib && bun run lint) && (cd web && bun run lint)",
        "build": "set -xe; (cd backend && bun run build) && (cd cli && bun run build) && (cd mclib && bun run build) && (cd web && bun run build)"
    }
}
